import { Vue, VueConstructor } from 'vue/types/vue';
import { Resource } from '@/core';
declare const _default: (resource: Resource) => {
    install: (Vue: VueConstructor<Vue>) => void;
};
export default _default;
